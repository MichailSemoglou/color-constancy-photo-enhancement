# All package metadata and build configuration has been moved to pyproject.toml
# (PEP 517/518).  This file is retained only for compatibility with older
# tooling that does not yet read pyproject.toml directly.
from setuptools import setup

setup()

